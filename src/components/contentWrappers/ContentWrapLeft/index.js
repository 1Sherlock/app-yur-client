import React from 'react';

const ContentWrapLeft = (props) => {
    return (
        <div className="card-wrap-left">
            {props.children}
        </div>
    );
};

export default ContentWrapLeft;